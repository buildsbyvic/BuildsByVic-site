# CLAUDE.md

Project-specific instructions for Claude Code when working in this repo.

## Git rules

- **Never run `git push` without asking the user for explicit confirmation first — every single time, no exceptions.** This applies even if a previous push was already approved in the same session; each push needs its own confirmation.
